{
  "name": "content-schema",
  "version": "1.0.0",
  "description": "This is a [Next.js](https://nextjs.org/) project bootstrapped with [`create-next-app`](https://github.com/vercel/next.js/tree/canary/packages/create-next-app).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate:types": "ts-node scripts/ts-generator.ts ./schemas",
    "generate:id": "ts-node scripts/cuid.ts",
    "generate:staging": "ts-node scripts/cuid.ts ../staging-content/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "cuid": "^3.0.0",
    "json-schema-to-typescript": "^15.0.0"
  }
}
